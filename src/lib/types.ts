export interface InvoiceData {
  senderName: string;
  friendName: string;
  address: string;
  reason: string;
  amount: string;
  dueDate: string;
  customMessage: string;
  invoiceId: string;
  issueDate: string;
}
